You've rsvpd!
<br />
<br />
It looks like you said that
<% if @user.is_attending %>
can
<% else %>
can't
<% end %>
attend.
<br />
If you need to change it, click the button below, but please email us at EMAIL if it's been more than a day since your previous rsvp.
<%= form_for @user do |f| %>
	<%= f.hidden_field :has_rsvpd, value: 'false' %>
	<%= f.submit %>
<% end %>
